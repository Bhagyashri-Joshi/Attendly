import { Router } from "express"; import { authenticate } from "../middleware/authenticate"; import { validateBody } from "../middleware/validate"; import { asyncHandler } from "../utils/asyncHandler"; import { attendanceByDate, createAttendance, deleteAttendance, listAttendance, updateAttendance } from "../controllers/attendanceController"; import { createAttendanceSchema, updateAttendanceSchema } from "../validators/attendanceValidators";
const router=Router(); router.use(authenticate); router.get("/",asyncHandler(listAttendance)); router.get("/date/:date",asyncHandler(attendanceByDate)); router.post("/",validateBody(createAttendanceSchema),asyncHandler(createAttendance)); router.put("/:id",validateBody(updateAttendanceSchema),asyncHandler(updateAttendance)); router.delete("/:id",asyncHandler(deleteAttendance)); export default router;
