import type { SubjectAnalytics } from '@/types/analytics';export function lecturesToReach75(s:SubjectAnalytics){if(s.percentage>=75)return 0;let x=0;while((s.present+x)/(s.total+x)<.75&&x<1000)x++;return x>=1000?null:x;}export function insightText(s:SubjectAnalytics){const x=lecturesToReach75(s);return x===null?`Improving ${s.name} will require a sustained attendance streak.`:x===0?`${s.name} is already at or above 75%.`:`You need to attend the next ${x} lectures in ${s.name} to reach 75%.`;}
