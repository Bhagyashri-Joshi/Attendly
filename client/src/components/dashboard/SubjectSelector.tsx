import type { SubjectAttendance } from "@/types/dashboard";
export function SubjectSelector({subjects,value,onChange}:{subjects:SubjectAttendance[];value:string;onChange:(v:string)=>void}){return <select value={value} onChange={e=>onChange(e.target.value)} className="mt-3 w-full rounded-lg border border-border bg-white px-3 py-2 text-sm"><option value="">Select Subject</option>{subjects.map(s=><option key={s.subjectId} value={s.subjectId}>{s.subjectName}{s.subjectCode?` (${s.subjectCode})`:""}</option>)}</select>}
