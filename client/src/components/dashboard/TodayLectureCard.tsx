import { Button } from "@/components/ui/button"; import type { TodayLecture } from "@/types/dashboard";
export function TodayLectureCard({lecture,onMark}:{lecture:TodayLecture;onMark:(s:"PRESENT"|"ABSENT")=>void}){return <div className="rounded-xl border border-border bg-white p-4"><div className="flex flex-col justify-between gap-3 sm:flex-row"><div><p className="font-medium text-forest-dark">{lecture.startTime} - {lecture.endTime}</p><h3 className="mt-1 font-display text-lg">{lecture.subjectName}</h3><p className="text-sm text-muted">{lecture.room||"Room not set"}</p></div><div className="flex items-center gap-2">{lecture.status?<span className="rounded-full bg-light-green px-3 py-1 text-xs font-medium text-forest">{lecture.status}</span>:<><Button className="text-xs" onClick={()=>onMark("PRESENT")}>Present</Button><Button variant="outline" className="text-xs" onClick={()=>onMark("ABSENT")}>Absent</Button></>}</div></div></div>}
