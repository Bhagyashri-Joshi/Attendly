import { useNavigate } from "react-router-dom"; import { Button } from "@/components/ui/button";
export function QuickActions(){const n=useNavigate();return <section className="rounded-card border border-border bg-white p-6"><h2 className="font-display text-xl text-forest-dark">Quick Actions</h2><div className="mt-4 grid grid-cols-1 gap-3 sm:grid-cols-2"><Button onClick={()=>n("/subjects")}>+ Add Subject</Button><Button variant="outline" onClick={()=>n("/timetable")}>+ Add Lecture</Button><Button variant="outline" onClick={()=>n("/attendance")}>Update Attendance</Button><Button variant="outline" onClick={()=>n("/analytics")}>View Analytics</Button></div></section>}
