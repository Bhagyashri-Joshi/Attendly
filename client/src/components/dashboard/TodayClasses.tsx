import type { TodayLecture } from "@/types/dashboard"; import { TodayLectureCard } from "./TodayLectureCard";
export function TodayClasses({lectures,onMark}:{lectures:TodayLecture[];onMark:(l:TodayLecture,s:"PRESENT"|"ABSENT")=>void}){return <section className="rounded-card border border-border bg-white p-6"><h2 className="font-display text-xl text-forest-dark">Today's Classes</h2>{lectures.length?<div className="mt-4 space-y-3">{lectures.map(l=><TodayLectureCard key={l.id} lecture={l} onMark={s=>onMark(l,s)}/>)}</div>:<div className="py-8 text-center text-muted">No classes scheduled for today. Create your timetable.</div>}</section>}
