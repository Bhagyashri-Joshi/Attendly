import type { OverallAnalytics } from '@/types/analytics';export function AnalyticsSummaryCards({data}:{data:OverallAnalytics}){const cards=[['Best Subject',data.bestSubject?.name??'—'],['Lowest Subject',data.lowestSubject?.name??'—'],['Overall Attendance',`${data.overallPercentage}%`],['Total Classes',String(data.totalLectures)]];return <div className="grid grid-cols-1 gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(([a,b])=><div key={a} className="rounded-card border border-border bg-white p-5"><p className="text-sm text-muted">{a}</p><p className="mt-2 font-display text-xl text-forest-dark">{b}</p></div>)}</div>}
