import type { AttendanceRecord } from "@/types/attendance"; interface P{records:AttendanceRecord[];onDelete:(id:string)=>void} export function AttendanceHistory({records,onDelete}:P){return <section className="mt-10"><h2 className="font-display text-xl text-forest-dark">Attendance History</h2>{records.length===0?<div className="mt-4 rounded-card border border-border bg-white p-8 text-center text-sm text-muted">No attendance records yet.<br/>Start tracking your lectures today.</div>:<div className="mt-4 space-y-2">{records.map(r=><div key={r.id} className="flex items-center justify-between rounded-card border border-border bg-white p-4 text-sm"><div><b>{r.subject.name}</b><span className="ml-2 text-muted">{String(r.date).slice(0,10)} · {r.timetableLectureId?"Scheduled Lecture":"Manual"}</span></div><div className="flex items-center gap-3"><span className={r.status==="PRESENT"?"text-forest":"text-[#B4533C]"}>{r.status}</span><button className="text-sm text-muted underline" onClick={()=>onDelete(r.id)}>Delete</button></div></div>)}</div>}</section>}
