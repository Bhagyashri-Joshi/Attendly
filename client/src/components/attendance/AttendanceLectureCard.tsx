import type { DailyLectureAttendance,AttendanceStatus } from "@/types/attendance"; import { AttendanceControls } from "./AttendanceControls"; interface P{item:DailyLectureAttendance;onChange:(s:AttendanceStatus)=>void} export function AttendanceLectureCard({item,onChange}:P){const l=item.lecture;return <article className="rounded-card border border-border bg-white p-5"><div className="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><h3 className="font-medium text-forest-dark">{l.subject.name} {l.subject.code&&<span className="text-muted">({l.subject.code})</span>}</h3><p className="mt-1 text-sm text-muted">{l.startTime} – {l.endTime}{l.room?` · Room ${l.room}`:""}</p></div><AttendanceControls status={item.status} onChange={onChange}/></div></article>}
